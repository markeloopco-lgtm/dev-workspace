# PSD正規化パイプライン用 (See-through本体の依存は含まない — docs/02参照)
psd-tools>=1.9
pytoshop>=1.2
six  # pytoshopが内部で使うが依存に宣言されていない
numpy
Pillow
PyYAML

# 参考動画の分析 (scripts/analyze_video.py — docs/06)。ffmpeg本体は別途インストール
yt-dlp[default,deno]
